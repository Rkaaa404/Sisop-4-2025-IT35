#include <stdio.h>
#include <string.h>
#include <stdlib.h>

// Dragon module: Placeholder for future implementation

// Transform path for dragon module
char* dragon_transform_path(const char *path, int to_source) {
    return strdup(path);
}

// Transform content for dragon module
void dragon_transform_content(char *buf, size_t size, int to_source) {
    // No transformation implemented yet
    return;
}